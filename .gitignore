# Local archive (huerfanos, originales raw, ZIPs, OSINT, prototipos legacy)
_archive/

# Work-in-progress drafts (refactors, experimentos, snapshots)
_drafts/

# Claude Code workspace settings
.claude/

# Internal CRUX-Dev artifacts (delivery reports, meeting minutes, OSINT notes).
# These contain "USO INTERNO" markers and lists of material we ask the client —
# not for the public repo. Add `!path/to/file.pdf` here if a public PDF is ever needed.
docs/
*.pdf

# Chat transcripts / scratchpads
chat*.txt

# Productivity plugin (internal task list, memory, dashboard — contains client context)
TASKS.md
CLAUDE.md
dashboard.html
memory/

# OS
.DS_Store
Thumbs.db
